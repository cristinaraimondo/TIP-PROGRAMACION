class Game_over extends Phaser.Scene {
    constructor() {
        super({ key: 'Game_over' });
    }

    init() {
        console.log('Scene: Game_over');
    }

    create() {}

    update(time, delta) {}
}

export default Game_over;
