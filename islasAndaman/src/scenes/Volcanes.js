class Volcanes extends Phaser.Scene {
    constructor() {
        super({ key: 'Volcanes' });
    }

    init() {
        console.log('Scene: Volcanes');
    }

    create() {}

    update(time, delta) {}
}

export default Volcanes;
